Execute (set complete function):
  autocmd FileType ps1 setlocal omnifunc=poshcomplete#CompleteCommand
  set ft=ps1


Given(ps1):
  get-comma

Do (invoke omnicomplete on 'get-comma'):
  A\<c-x>\<c-o>\<Esc>

Expect (should complete word):
  Get-Command

# vim:set et ts=2 sts=0 sw=2 ff=unix:

